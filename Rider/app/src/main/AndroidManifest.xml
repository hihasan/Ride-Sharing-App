<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hihasan.khalikoi.rider">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

<!--android:name=".StartupActivity"-->

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NoInternet"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--<activity-->
            <!--android:name=".MainActivity"-->
            <!--android:theme="@style/AppTheme.NoActionBar" />-->

        <!-- Sign in & Up -->
        <activity
            android:name=".signin.SignInMain"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".signup.SignupMain"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName=".signin.SignInMain"/>

        <!--HELP-->
        <activity android:name=".help.Help"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".help.ChatActivity"
            android:parentActivityName=".MainActivity"/>

        <!--Settings-->
        <activity android:name=".settings.Settings"

            android:parentActivityName=".MainActivity"/>


    </application>

</manifest>